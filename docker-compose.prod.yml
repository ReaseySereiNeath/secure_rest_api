version: '3.9'

services:
  api:
    build: .
    container_name: secure_rest_api_prod
    env_file:
      - .env.prod
    ports:
      - "8000:8000"
    command: [
      "uvicorn",
      "main:app",
      "--host",
      "0.0.0.0",
      "--port",
      "8000"
    ]
    restart: always
